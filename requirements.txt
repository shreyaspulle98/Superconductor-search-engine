# Requirements for training semantic search model on MacBook
# Install with: pip install -r requirements.txt

# Core deep learning (with MPS support for Mac M1/M2/M3/M4)
torch>=2.0.0
transformers>=4.30.0
sentence-transformers>=2.2.0

# Data processing
numpy>=1.24.0

# Progress bars
tqdm>=4.65.0

# For evaluation (optional but recommended)
scikit-learn>=1.3.0

# Required for sentence-transformers training
datasets>=2.0.0

# Note: torch will automatically use MPS (Metal Performance Shaders)